import { FeaturePlaceholder } from "@/components/shared/FeaturePlaceholder";

export function DetectionPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Object detection"
      title="The vision pipeline will arrive in Phase 5."
      description="The route and OpenCV/ONNX boundaries are prepared for preprocessing, predictions, confidence scores, and visual overlays."
    />
  );
}
