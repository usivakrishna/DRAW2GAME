import { FeaturePlaceholder } from "@/components/shared/FeaturePlaceholder";

export function JsonEditorPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Level definition"
      title="The live JSON editor will arrive in Phase 6."
      description="The canonical level schema is already established so detection and AI edits share one validated level contract."
    />
  );
}
