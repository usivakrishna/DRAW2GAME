import { FeaturePlaceholder } from "@/components/shared/FeaturePlaceholder";

export function DashboardPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Project dashboard"
      title="Metrics and recent projects will arrive in Phase 9."
      description="The global dashboard route is ready for generated-game, upload, accuracy, processing-time, and recents summaries."
    />
  );
}
